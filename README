Sgit: git minifier

Sgit is a git minifier written in D.

sgit -i (initialize new git repository)

sgit -r REMOTE (optional)

sgit -b "NAME" (required if initializing)

You can install Sgit by typing

gitstrap install sgit

OR 

sudo make install